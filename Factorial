#include<iostream>
#include<conio.h>
using namespace std;

int factorial(int no){
	
	if(no>1){	
		return no * fact(no-1);
	}
	else{
		return 1;
	}
}


void main(){
	
	int no;
	
	cout<<"Enter Any Number :"; 
  cin>>no;
	
	cout<<factorial(no);
	getch();
}

